case_id: mb_ffi_c_unwind_callback_panic
suite: micro
category: controlled

source:
  crate_name: mb_ffi_c_unwind_callback_panic
  version: local
  advisory: null
  fixed_version: null
  url: null

ground_truth:
  primary_label: InsideUnsafePanic
  relation: FfiBoundary
  oracle_verdict: Unknown
  harness_status: LikelyValid
  security_relevant: true
  oracle_confirmable: false
  expected_reached_count: 1

dangerous_categories:
  - FfiBoundary
panic_kinds:
  - PanicMacro
harness:
  path: fuzz/fuzz_targets/fuzz_target_1.rs
  validity_rationale: fuzz target calls a safe wrapper; the wrapper enters an extern C-unwind callback boundary
selection:
  reason: adds another FFI boundary relation case using a callback rather than a direct extern function call
  negative_case: false
  manually_labeled: true
notes:
  - This case is intentionally pure Rust so it remains portable while still exercising C-unwind boundary logic.
