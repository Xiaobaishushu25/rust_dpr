case_id: tb_harness_direct_unsafe_precondition
suite: taxonomy
category: harness-misuse-taxonomy

source:
  crate_name: tb_harness_direct_unsafe_precondition
  version: local
  advisory: null
  fixed_version: null
  url: null

ground_truth:
  primary_label: HarnessMisuse
  relation: HarnessMisuse
  oracle_verdict: Unknown
  harness_status: LikelyMisuse
  security_relevant: false
  oracle_confirmable: false
  expected_reached_count: 0

dangerous_categories:
  - RawPointer
  - AllocationOwnership
panic_kinds: []
harness:
  path: fuzz/fuzz_targets/fuzz_target_1.rs
  validity_rationale: fuzz target directly invokes an unsafe API with a bogus length that violates the documented precondition
selection:
  reason: taxonomy case for direct unsafe API misuse rather than a library vulnerability
  negative_case: true
  manually_labeled: true
notes:
  - Valid callers should use safe_sum; the intentionally invalid harness call must be rejected as misuse.
