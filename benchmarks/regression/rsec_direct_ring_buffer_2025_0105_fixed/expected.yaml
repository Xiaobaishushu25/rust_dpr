case_id: rsec_direct_ring_buffer_2025_0105_fixed
suite: regression
category: regression-official-fixed-control
schema_version: 3

source:
  crate_name: direct_ring_buffer
  vulnerable_version: "<=0.2.1"
  fixed_version: "=0.2.2"
  advisory: RUSTSEC-2025-0105
  cve: null
  ghsa: GHSA-fp5x-7m4q-449f
  url: https://rustsec.org/advisories/RUSTSEC-2025-0105.html
  upstream_issue: https://github.com/ain1084/direct_ring_buffer/issues/1
  upstream_pr: https://github.com/ain1084/direct_ring_buffer/pull/2
  external_crate: true
  affected_function: direct_ring_buffer::create_ring_buffer

ground_truth:
  primary_label: DangerousPathReached
  relation: NoneObserved
  oracle_verdict: NoOracleFinding
  harness_status: LikelyValid
  security_relevant: false
  oracle_confirmable: true
  expected_reached_count: 1

dangerous_categories:
  - RawPointer
  - AllocationInitialization
  - TypeValidityInvariant
panic_kinds: []

harness:
  path: fuzz/fuzz_targets/fuzz_target_1.rs
  validity_rationale: |
    The harness calls the real fixed direct_ring_buffer 0.2.2 public API.
    It uses a safe bool buffer and does not fabricate raw pointers,
    uninitialized memory, invalid lengths, or ownership states.

selection:
  reason: |
    Official fixed-version control for RUSTSEC-2025-0105. Version 0.2.2
    initializes the internal allocation with T::default before exposing
    typed slices, so the same bool-slice access pattern should have no
    Miri undefined-behavior finding.
  negative_case: true
  manually_labeled: true

controls:
  fixed_version_case: null
  vulnerable_version_case: rsec_direct_ring_buffer_2025_0105_vuln

execution:
  deterministic_test: fixed_version_initializes_bool_buffer
