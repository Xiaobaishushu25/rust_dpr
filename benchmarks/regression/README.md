# Regression Benchmark

Each case must include:

- vulnerable version or local minimized crate;
- expected.yaml using schema v2;
- reproducer or deterministic test;
- source advisory/issue URL;
- fixed version if available.

Cases should be selected from RustSec, CVE entries, or public GitHub issues.
