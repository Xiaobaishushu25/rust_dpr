[package]
name = "rw_postcard_deser"
version = "0.1.0"
edition = "2021"
publish = false

[dependencies]
rustdpr-trace = { path = "../../../crates/rustdpr-trace" }
postcard = { version = "=1.1.3", features = ["alloc", "use-std"] }
serde = { version = "=1.0.228", features = ["derive"] }
